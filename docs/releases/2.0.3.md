# Poetize v2.0.3

主站与聊天室使用一致的登录令牌规则：空值以及字符串 `null`、`undefined` 不会作为有效会话使用。聊天室在浏览器存储不可用时安全清理本地会话并返回主站，避免登录导航因存储异常中断。

两个前端现在提供登录令牌测试；正式发布流程执行这些测试、lint、前端构建和后端 Maven 构建。Linux x86-64 部署包及 SHA-256 校验文件由标签工作流生成。
